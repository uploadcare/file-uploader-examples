{
  "name": "js-uploader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "serve -p 8080",
    "build": "rm -rf dist && mkdir dist && cp index.html minimal.html regular.html dist"
  },
  "dependencies": {
    "@codesandbox/nodebox": "0.1.9",
    "@uploadcare/blocks": "0.44.0"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
